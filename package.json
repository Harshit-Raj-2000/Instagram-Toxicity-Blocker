{
  "dependencies": {
    "@tensorflow-models/toxicity": "^1.2.2",
    "@tensorflow/tfjs": "1.0",
    "parcel": "*"
  },
  "scripts": {
    "copy": "cp content.js dist/ && cp style.css dist/",
    "build": "parcel build background.js && npm run copy"
  }
}
